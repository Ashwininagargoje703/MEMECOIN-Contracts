# My Meme Coin Launcher

## Setup

1. Clone & install:
   ```bash
   git clone <repo-url>
   cd my-meme-launcher
   npm install
   cp .env.example .env
   # Fill in RPC_URL and PRIVATE_KEY in .env# MEMECOIN-Contracts
